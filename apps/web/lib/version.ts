/**
 * Application version — single source of truth.
 *
 * Rules:
 * - Bump +0.001 on every deploy/update.
 * - Keep in sync with deploy/VERSION and deploy/DEPLOY.md.
 */
export const APP_VERSION = "1.001";
export const APP_NAME = "Sass Store";
