{"message":"Database error during get_pending_retouches: Error al calcular retouches pendientes: Failed query: \n        SELECT \n          c.id AS \"customerId\", \n          c.name AS \"customerName\", \n          c.phone AS \"customerPhone\", \n          cv.visit_date AS \"visitDate\"\n        FROM customers c\n        INNER JOIN customer_visits cv ON cv.customer_id = c.id AND cv.status = 'completed'\n        WHERE c.tenant_id = $1 AND c.status = 'active'\n        ORDER BY cv.visit_date DESC\n      \nparams: 0aa4afad-e647-49c6-8b08-74d1b4bedea2","type":"DatabaseError"}