/**
 * AgentBrain — orquestador del agente por tenant
 *
 * Recibe el intent clasificado y la sesión activa,
 * decide la respuesta (hybrid engine) y la retorna.
 * Los handlers de n8n llaman a este módulo vía API interna.
 */
export class AgentBrain {
  // TODO(B5): conectar HybridResponseEngine + Claude API
}
