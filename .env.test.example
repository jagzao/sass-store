# Test Environment Configuration
# Copy this to .env.test and configure a SEPARATE test database

# ⚠️ CRITICAL: Use a SEPARATE database for tests
# DO NOT use your production database URL here!
TEST_DATABASE_URL="postgresql://user:password@localhost:5432/sass_store_test"

# Or use a separate Supabase project for testing
# TEST_DATABASE_URL="postgresql://postgres:[password]@[project-ref].supabase.co:5432/postgres"

# All other env vars will be inherited from .env
